events {}

http {
    server {
        listen 8081;
        server_name localhost;

        root /usr/share/nginx/html/siteB;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}

