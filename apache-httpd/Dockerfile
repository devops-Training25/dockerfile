# Use the official Apache HTTP Server image
FROM httpd:2.4

# Optional: copy custom HTML into the web root
COPY ./index.html /usr/local/apache2/htdocs/index.html

# Expose default HTTP port
EXPOSE 80
